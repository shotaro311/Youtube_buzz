/** @type {import('prettier').Config} */
const config = {
  arrowParens: "avoid",
  printWidth: 100,
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: "all",
};

export default config;
